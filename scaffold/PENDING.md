admin页面前端实现user和active code页面的filter功能。

admin页面只有用户身份是agent和admin才能浏览，用户的edit，agent和admin都可以，active code的view限定agent和admin，active code create 限定admin。

## still thinking, do not touch
添加 CI/CD 管道

!!! bug：网页明明开着，但是token到期，refresh token没有fresh成功



-- 读取流
本地向量化FastEmbed
