

-- [x] Create one folder for front end and one for back end.
-- [x] init those npm packages.
-- [x] 安装并配置 MUI (Material UI) 作为前端 UI 组件库和主题系统, yarn add all lib for nextjs.
-- [x] git init this project and add gitignore files to two sub project
-- [x] add all lib for backend, add all for nodejs, fastiy and mongodb client.
-- [x] add test run the nextjs to see if it able to buid for dev.
-- [x] add one test route for fastiy and test if fastiy dev work well.
-- [x] add package script to main root package and run dev with both gui and backend
-- [x] add .env file to each sub project. and change all variable with .env vairable.

## 主页排版

-- [x] 创建主页基础布局：左右两栏结构（左侧导航栏 + 中间 Panel）
-- [x] 实现左侧导航栏收起状态：仅显示展开 icon 和设置 icon
-- [x] 实现左侧导航栏展开/收起动画与状态切换
-- [x] 实现左侧导航栏展开时，在收起icon左边显示title “AI 实时翻译”
-- [x] 实现左侧导航栏展开状态：显示 topic 列表、新增 topic 入口，setting icon 向右平移
-- [x] topic list有最高高度，超过最高高度，除了title部分和setting icon部分之后的80%高度。当超过最高高度时，需要展示滚动条。添加一些fake topic来测试。
-- [ ] 实现中间 Panel 上下两部分布局（主显示区占大部分高度，输入区固定底部）
-- [ ] 实现主显示区：展示音频状态、原文、译文
-- [ ] 实现底部输入区：文字输入框 + 麦克风按钮 + 提交按钮